import Signup from "@/components/Signup";
import { FormEvent } from "react";
import { z } from "zod";

const page = () => {
  return (
    <div>
      <Signup />
    </div>
  );
};

export default page;
